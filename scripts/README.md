# Automated Utility Scripts

Place operational command utilities, seeding pipelines, or staging build helpers here.
For example:
- `scripts/database-reset.sh`: Bash wrapper to drop tables, migrate, and run seed scripts.
- `scripts/deploy-staging.sh`: Pipeline instructions to upload assets and trigger restarts.
