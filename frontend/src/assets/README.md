# Static Assets Directory

Store local static assets like SVG icons, corporate logos, background graphics, and fonts here.
For example, import images directly in React:

```typescript
import brandLogo from '@/assets/logo.svg';
```
